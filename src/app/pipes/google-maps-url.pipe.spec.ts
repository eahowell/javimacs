import { GoogleMapsUrlPipe } from './google-maps-url.pipe';

describe('GoogleMapsUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new GoogleMapsUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
